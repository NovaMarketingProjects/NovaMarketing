---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { buildAlternates } from '../../lib/i18n';
import { strapiClient } from '../../lib/strapi';
import LogoMarquee from '../../components/sections/LogoMarquee.astro';
import FaqHome from '../../components/sections/FaqHome.astro';

const SITE_URL = import.meta.env.PUBLIC_SITE_URL || 'https://novamarketing.es';
const alternates = buildAlternates('/agencia-seo-en-terrassa', '/ca/agencia-seo-a-terrassa', SITE_URL);

const faqItems = [
  {
    question: "Com pot el SEO ajudar la meva empresa a Terrassa a aconseguir més clients?",
    answer: "Terrassa té un teixit empresarial molt actiu, amb un sector industrial fort i una creixent activitat de serveis. El SEO local a Terrassa et permet aparèixer quan algú a la ciutat o a l'eix Terrassa-Sabadell cerca el que tu ofereixes. És la forma més rendible de captar clients que ja tenen intenció de comprar."
  },
  {
    question: "El SEO funciona per a empreses industrials del polígon de Terrassa?",
    answer: "Sí. Les empreses del polígon industrial de Terrassa —ja siguin fabricants, distribuïdors o serveis tècnics— tenen clients que cerquen proveïdors a Google. El SEO B2B per a indústria a Terrassa posiciona la teva empresa per cerques professionals que generen pressupostos i contractes reals, no només visites."
  },
  {
    question: "Quant de temps tarda el SEO a posicionar a Terrassa?",
    answer: "A Terrassa, amb un nivell de competència moderat en la majoria de sectors, els primers resultats solen veure's en 3-5 mesos. Sectors com clíniques, immobiliàries o serveis jurídics poden requerir una mica més de temps. L'important és que el SEO genera un creixement sostingut i acumulatiu."
  },
  {
    question: "Treballeu el SEO local per a negocis de Ca n'Anglada i Vallparadís?",
    answer: "Sí. Per a negocis en barris com Ca n'Anglada, Vallparadís o el centre de Terrassa, optimitzem la presència a Google Maps i creem continguts que apunten a cerques locals específiques. L'objectiu és que quan algú al teu barri o a Terrassa cerqui el teu servei, el teu negoci sigui el primer en aparèixer."
  },
  {
    question: "El SEO pot ajudar a negocis propers a la Universitat Politècnica de Terrassa?",
    answer: "Sens dubte. L'entorn universitari genera una gran demanda de serveis: alimentació, hostaleria, reprografies, tecnologia, formació complementària. Les empreses ben posicionades a Google capten aquest trànsit de forma contínua. Dissenyem estratègies que connecten el teu negoci amb el públic universitari i professional de Terrassa."
  }
];

// Casos d'èxit SEO
let seoCases: any[] = [];
try {
  const casesRes = await strapiClient.getCaseStudies('ca');
  seoCases = casesRes.data
    .map((i: any) => i.attributes ?? i)
    .filter((c: any) => {
      const cats = (c.categories || []).map((cat: any) => (cat.slug || cat.attributes?.slug || '').toLowerCase());
      return cats.some((s: string) => s.includes('seo') || s.includes('consultor'));
    });
} catch {}

const steps = [
  { title: "Auditoria SEO a Terrassa", desc: "Analitzem la teva presència digital a Terrassa i detectem els errors tècnics que frenen la teva visibilitat. Estudiem la teva competència local i tracem el camí per superar-la a Google." },
  { title: "Keyword Research Local", desc: "Identifiquem les cerques que fan els usuaris de Terrassa, Ca n'Anglada, Vallparadís i l'eix Terrassa-Sabadell per atreure trànsit amb alta intenció de compra al teu negoci." },
  { title: "SEO On-Page per al Teu Sector", desc: "Adaptem els continguts i l'estructura del teu web per posicionar-te a Terrassa. Tant si ets un negoci de serveis com una empresa industrial, optimitzem per al teu client real." },
  { title: "Linkbuilding al Vallès", desc: "Construïm l'autoritat del teu domini amb enllaços de qualitat des de mitjans locals del Vallès Occidental, directoris empresarials de Terrassa i webs de referència del teu sector." },
  { title: "SEO Local i Google Maps", desc: "Posicionem el teu Google Business Profile a Terrassa perquè apareguis a les cerques locals amb mapa. Essencial per a negocis de proximitat, hostaleria, clíniques i serveis al client." }
];
---

<BaseLayout
  title="Agència SEO a Terrassa | Nova Marketing"
  description="Posicionem la teva empresa a Google per dominar les cerques a Terrassa. Des de Ca n'Anglada fins al polígon industrial. Auditoria SEO gratis."
  locale="ca"
  alternates={alternates}
>

    <!-- ═══ HERO / H1 ═══════════════════════════════════════════════════════ -->
    <section class="min-h-[88vh] flex items-center justify-center pt-24 pb-16 px-4 sm:px-6 relative overflow-hidden text-center bg-white border-b border-black/5">
        <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808008_1px,transparent_1px),linear-gradient(to_bottom,#80808008_1px,transparent_1px)] bg-[size:24px_24px] [mask-image:radial-gradient(ellipse_50%_50%_at_50%_40%,#000_75%,transparent_100%)]"></div>
        <div class="absolute -top-10 left-1/4 w-72 h-72 bg-accent/15 rounded-full blur-3xl opacity-30 animate-pulse"></div>
        <div class="absolute top-20 right-1/4 w-80 h-80 bg-purple-500/5 rounded-full blur-3xl opacity-20"></div>

        <div class="max-w-7xl mx-auto reveal relative z-10">
            <span class="inline-flex items-center gap-2 bg-accent/5 backdrop-blur-sm px-4 py-2 border border-accent/10 rounded-full text-accent text-[8px] sm:text-[10px] font-black uppercase tracking-[0.3em] mb-6 shadow-sm">
                <span class="w-1.5 h-1.5 bg-accent rounded-full animate-ping"></span>
                Especialistes en Posicionament SEO a Terrassa
            </span>

            <h1 class="text-4xl sm:text-6xl md:text-7xl lg:text-[5.5rem] uppercase font-black tracking-tighter leading-[0.82] mb-8 text-zinc-900 flex flex-col items-center text-center">
                <span>Agència de</span>
                <span>Posicionament SEO</span>
                <span class="text-mktg-gradient px-4 overflow-visible inline-block">a Terrassa</span>
            </h1>

            <p class="text-base sm:text-lg text-zinc-500 font-light leading-relaxed max-w-2xl mx-auto mb-12">
                Posicionem la teva empresa a Google per dominar les cerques a Terrassa. Des de Ca n'Anglada fins al polígon industrial, connectem el teu negoci amb els clients que busquen exactament el que ofereixes.
            </p>

            <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
                <button class="contact-modal-trigger btn-mktg-v5 shadow-[0_20px_40px_-10px_rgba(0,0,0,0.15)] hover:shadow-[0_25px_50px_-12px_rgba(0,0,0,0.25)] hover:-translate-y-1 transition-all duration-300 !py-5 !px-12 !text-[12px] !tracking-wider font-black w-full sm:w-auto">
                    SOL·LICITAR AUDITORIA GRATIS
                </button>
            </div>
        </div>
    </section>

    <!-- ═══ LOGOS CLIENTES (SOCIAL PROOF) ══════════════════════════════════ -->
    <LogoMarquee />

    <!-- ═══ SECCIÓN 1: AUDITORÍA Y ESTRATEGIA ═══════════════════════════════ -->
    <section class="py-20 sm:py-32 bg-zinc-50 border-b border-black/5 px-4 sm:px-6 relative overflow-hidden">
        <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808005_1px,transparent_1px),linear-gradient(to_bottom,#80808005_1px,transparent_1px)] bg-[size:24px_24px] [mask-image:radial-gradient(ellipse_50%_50%_at_50%_50%,#000_80%,transparent_100%)]"></div>

        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-16 sm:mb-24 reveal">
                <h2 class="text-3xl sm:text-5xl lg:text-7xl font-extrabold tracking-tighter uppercase mb-6 text-zinc-900 leading-[0.82]">
                    Auditoria i Estratègia <br class="hidden sm:inline" /> SEO a Terrassa
                </h2>
                <p class="text-zinc-500 font-light text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">
                    No comencem a cegues ni fem servir plantilles genèriques. Analitzem la teva presència digital a Terrassa de dalt a baix per dissenyar una estratègia orientada 100% a conversions al mercat local del Vallès.
                </p>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 items-stretch">
                {[
                    { title: "Auditoria Tècnica Local", desc: "Corregim errors d'indexació, velocitat i adaptabilitat mòbil perquè Google et posicioni com la millor opció a Terrassa i el seu entorn industrial." },
                    { title: "Keywords de Terrassa", desc: "Analitzem què cerquen els clients de Terrassa, Ca n'Anglada, Vallparadís i l'eix Terrassa-Sabadell per posicionar termes amb intenció de compra real." },
                    { title: "SEO On-Page", desc: "Optimitzem títols, etiquetes i enllaçat intern per dir-li a Google exactament què vens, ja siguis un negoci de serveis o una empresa industrial a Terrassa." },
                    { title: "Pla d'Enllaços", desc: "Construïm autoritat amb enllaços de mitjans locals del Vallès Occidental i directoris empresarials de Terrassa que disparen la rellevància del teu domini." }
                ].map((s, i) => (
                    <div class="group flex flex-col justify-between p-8 sm:p-10 h-full bg-white rounded-3xl border border-zinc-100/80 shadow-sm hover:shadow-2xl hover:-translate-y-2 hover:border-accent/10 transition-all duration-500 relative overflow-hidden reveal flex-1">
                        <div class="absolute inset-x-0 h-1 bottom-0 bg-gradient-to-r from-accent via-accent/90 to-purple-600 w-0 group-hover:w-full transition-all duration-500"></div>
                        <div class="absolute -bottom-6 -right-4 text-9xl font-black text-black/2 group-hover:text-accent/[0.04] transition-all duration-500 select-none">
                            0{i + 1}
                        </div>
                        <div class="relative z-10 flex flex-col h-full justify-between">
                            <div>
                                <div class="p-3 bg-zinc-50 border border-black/5 w-fit rounded-xl mb-6 group-hover:bg-accent/5 transition-colors">
                                    <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path></svg>
                                </div>
                                <h3 class="text-xl sm:text-2xl uppercase mb-3 tracking-tighter group-hover:text-accent font-black text-zinc-900 border-b border-zinc-100 pb-2 w-fit">{s.title}</h3>
                                <p class="text-xs sm:text-sm text-zinc-500 leading-relaxed font-light">{s.desc}</p>
                            </div>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- ═══ SECCIÓN 2: TIMELINE ═══════════════════════════════════════════════ -->
    <section class="py-20 sm:py-32 bg-white px-4 sm:px-6 relative overflow-hidden border-b border-black/5">
        <div class="max-w-5xl mx-auto">
            <div class="text-center mb-16 sm:mb-24 reveal">
                <h2 class="text-2xl sm:text-5xl lg:text-7xl font-black tracking-tighter uppercase mb-6 text-zinc-900 leading-[0.85]">
                    Com treballem el SEO a Terrassa
                </h2>
                <div class="text-zinc-500 w-[75%] mx-auto font-light text-sm sm:text-base leading-relaxed space-y-4">
                    <p>
                        Oblida't del SEO en pilot automàtic. Comptaràs amb un <strong>Account Manager dedicat</strong> que treballarà de la mà amb el teu equip per resoldre dubtes, supervisar avenços i mantenir-te informat amb mètriques de negoci reals a Terrassa.
                    </p>
                </div>
            </div>

            <div id="timeline-container" class="relative timeline-container">
                <!-- Vertical Background Line -->
                <div class="hidden md:block absolute left-1/2 top-4 bottom-4 w-px bg-zinc-200 -translate-x-1/2"></div>
                <!-- Vertical Progressing Line -->
                <div class="progress-bar hidden md:block absolute left-1/2 top-4 w-px bg-accent -translate-x-1/2 transition-all duration-100" style="height: 0%;"></div>

                <div class="space-y-16 md:space-y-24 flex flex-col items-center">
                    {steps.map((step, index) => (
                        <div class="flex flex-col md:flex-row items-center gap-6 md:gap-12 w-full relative reveal" style={`transition-delay: ${index * 100}ms`}>

                            <!-- Borde Lateral Izquierdo (Desktop Solo Alternancia) -->
                            <div class="hidden md:block md:flex-1 text-right md:order-1">
                                {index % 2 === 0 && (
                                    <div class="md:pr-12">
                                        <h3 class="text-xl sm:text-2xl font-black uppercase tracking-tighter mb-2 text-zinc-900">{step.title}</h3>
                                        <p class="text-sm text-zinc-500 font-light max-w-md ml-auto">{step.desc}</p>
                                    </div>
                                )}
                            </div>

                            <!-- Nodo Central -->
                            <div class="md:order-2 w-12 h-12 rounded-full border-2 border-zinc-200 bg-white flex items-center justify-center font-black text-lg z-10 shrink-0 shadow-sm node-circle transition-all duration-300">
                                0{index + 1}
                            </div>

                            <!-- Lado Derecho (Móvil todo / Desktop Impares) -->
                            <div class="flex-1 text-left md:order-3">
                                <div class="md:pl-12">
                                    <h3 class:list={["text-xl sm:text-2xl font-black uppercase tracking-tighter mb-2 text-zinc-900", index % 2 === 0 ? "md:hidden" : ""]}>{step.title}</h3>
                                    <p class:list={["text-sm text-zinc-500 font-light max-w-md mr-auto", index % 2 === 0 ? "md:hidden" : ""]}>{step.desc}</p>
                                </div>
                            </div>

                        </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    <style>
        .timeline-container .reveal {
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.6s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        .timeline-container .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
    </style>

    <script is:inline>
        function initTimeline() {
            const timeline = document.getElementById('timeline-container');
            const progressBar = timeline?.querySelector('.progress-bar');

            function updateProgress() {
                if (!timeline || !progressBar) return;
                const rect = timeline.getBoundingClientRect();
                const winHeight = window.innerHeight;
                const topPassed = (winHeight / 2) - rect.top;
                let percentage = (topPassed / rect.height) * 100;
                percentage = Math.max(0, Math.min(100, percentage));
                progressBar.style.height = `${percentage}%`;

                const nodes = timeline.querySelectorAll('.node-circle');
                nodes.forEach((node) => {
                    const nodeTop = node.getBoundingClientRect().top;
                    if (nodeTop < winHeight / 2) {
                        node.classList.add('!border-accent', '!bg-accent', '!text-white');
                    } else {
                        node.classList.remove('!border-accent', '!bg-accent', '!text-white');
                    }
                });
            }

            window.addEventListener('scroll', updateProgress);
            document.addEventListener('astro:page-load', updateProgress);
            updateProgress();
        }
        document.addEventListener('DOMContentLoaded', initTimeline);
        document.addEventListener('astro:page-load', initTimeline);
    </script>

    <!-- ═══ CASOS D'ÈXIT SEO ════════════════════════════════════════════ -->
    {seoCases.length > 0 && (
    <section class="py-20 sm:py-32 bg-zinc-50 border-t border-black/5 px-4 sm:px-6">
        <div class="max-w-7xl mx-auto">
            <div class="mb-16 reveal">
                <span class="text-[10px] font-black uppercase tracking-[0.3em] text-accent mb-4 block">Resultats provats</span>
                <h2 class="text-3xl sm:text-5xl lg:text-6xl uppercase tracking-tighter font-extrabold text-zinc-900 leading-[0.9]">
                    Casos d'èxit de posicionament<br class="hidden md:inline" /> SEO a Terrassa
                </h2>
            </div>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
                {seoCases.map((cs: any, i: number) => {
                  const catName = (cs.categories || []).map((c: any) => c.name || c.attributes?.name).filter(Boolean).join(' | ');
                  const caseUrl = cs.isPublic && cs.slug ? `/ca/casos-exit/${cs.slug}/` : null;
                  return (
                    <div class="bg-white p-8 border border-black/5 flex flex-col justify-between group hover:shadow-xl hover:-translate-y-1 transition-all duration-300 rounded-2xl reveal" style={`transition-delay: ${i * 80}ms`}>
                      <div>
                        {catName && <span class="text-[10px] font-black uppercase tracking-widest text-accent mb-4 block">{catName}</span>}
                        <h3 class="text-base font-black uppercase tracking-tight text-zinc-900 mb-3 group-hover:text-accent transition-colors leading-tight">{cs.title}</h3>
                        <p class="text-xs text-zinc-500 font-light leading-relaxed">{cs.description}</p>
                      </div>
                      {caseUrl && (
                        <a href={caseUrl} class="mt-6 text-[9px] font-black tracking-[0.25em] text-accent flex items-center gap-1.5 hover:gap-3 transition-all uppercase">
                          VEURE MÉS <span class="text-sm">→</span>
                        </a>
                      )}
                    </div>
                  );
                })}
            </div>
        </div>
    </section>
    )}

    <!-- ═══ FAQ ════════════════════════════════════════════════════════════ -->
    <FaqHome
        items={faqItems}
        title="Preguntes sobre posicionament SEO a Terrassa<br/>que ens fan altres pimes"
    />

</BaseLayout>
